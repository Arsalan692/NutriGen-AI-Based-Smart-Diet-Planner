# ──────────────────────────────────────────────
#  NutriGen Dark Theme — shared design tokens
# ──────────────────────────────────────────────

# ── Background layers ───────────────────────
BG_DARK    = "#0b0f14"        # deepest background
BG_MID     = "#111820"        # sidebar / panels
BG_CARD    = "#161d27"        # card surface
BG_CARD2   = "#1c2530"        # card hover / alt
BG_INPUT   = "#1a2231"        # input fields

# ── Accent palette ──────────────────────────
EMERALD    = "#10b981"        # primary accent
EMERALD_L  = "#34d399"        # lighter accent
EMERALD_D  = "#059669"        # darker accent
EMERALD_BG = "#10b98118"      # subtle tint

AMBER      = "#f59e0b"        # calories / warnings
AMBER_L    = "#fbbf24"
SKY        = "#38bdf8"        # carbs
ROSE       = "#fb7185"        # danger / fats
VIOLET     = "#a78bfa"        # BMI / extras
LIME       = "#a3e635"        # fiber

# ── Text ────────────────────────────────────
TEXT_PRIMARY   = "#f1f5f9"    # headings / important
TEXT_SECONDARY = "#94a3b8"    # body text
TEXT_MUTED     = "#64748b"    # subtle / labels
TEXT_ON_ACCENT = "#022c22"    # text on emerald bg

# ── Borders ─────────────────────────────────
BORDER       = "#1e293b"
BORDER_LIGHT = "#334155"

# ── Meal accent colors ─────────────────────
MEAL_ACCENT = {
    "Breakfast": "#fb923c",
    "Lunch":     "#10b981",
    "Dinner":    "#38bdf8",
    "Snack 1":   "#fbbf24",
    "Snack 2":   "#f472b6",
}

MEAL_ICON = {
    "Breakfast": "☀️",
    "Lunch":     "🍱",
    "Dinner":    "🌙",
    "Snack 1":   "🥜",
    "Snack 2":   "🍎",
}

# ── Fonts (LARGER for readability) ──────────
F_LOGO     = ("Segoe UI", 26, "bold")
F_HERO     = ("Segoe UI", 46, "bold")
F_HERO_SUB = ("Segoe UI", 17)
F_H1       = ("Segoe UI", 24, "bold")
F_H2       = ("Segoe UI", 18, "bold")
F_H3       = ("Segoe UI", 16, "bold")
F_BODY     = ("Segoe UI", 14)
F_BODY_B   = ("Segoe UI", 14, "bold")
F_SMALL    = ("Segoe UI", 13)
F_SMALL_B  = ("Segoe UI", 13, "bold")
F_TINY     = ("Segoe UI", 12)
F_NUM_BIG  = ("Segoe UI", 34, "bold")
F_NUM      = ("Segoe UI", 22, "bold")
F_MONO     = ("Consolas", 13)
F_NAV      = ("Segoe UI", 14, "bold")
F_NAV_ICON = ("Segoe UI", 18)
